# YahNeeQ Content Studio

Run the editor locally:

```powershell
cd studio
bun install
bun run dev
```

Sign in with the Sanity account that owns project `7n2t0tql`.

To host the editor through Sanity:

```powershell
bun run deploy
```

The public website reads published projects and blog posts from the `production` dataset.
